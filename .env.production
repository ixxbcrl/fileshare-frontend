# Production Environment Variables
# Copy this file to .env.production.local and configure for your Raspberry Pi

# API URL for production deployment
# Empty string means use relative URLs - nginx will proxy to backend
# This allows the app to work regardless of how you access it (IP, hostname, etc.)
VITE_API_URL=
